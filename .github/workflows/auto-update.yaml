name: auto-update

on:
  schedule:
    - cron: '48 15 * * 0,3,5,6'  # 3:48 pm UTC on Sundays, Wednesdays, Fridays, Saturdays
  workflow_dispatch:

jobs:
  auto-update:
    uses: memo33/sc4pac-actions/.github/workflows/sc4pac-channel-updates.yaml@main
    with:
      path: src/yaml
      api: sc4e  # sc4e,stex
      deploy-repository: memo33/sc4pac
    secrets:
      stex-api-key: ${{ secrets.STEX_API_KEY }}
      pat: ${{ secrets.SC4PAC_CHECK_UPDATES_TOKEN }}
    permissions:
      pull-requests: write
